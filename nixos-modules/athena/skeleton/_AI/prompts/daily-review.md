End-of-day review for today's daily note (`Daily/YYYY-MM-DD.md`, today's
date).

1. Read today's daily note.
2. Also scan `Inbox/` for items created today that weren't triaged — if any,
   triage them now (file into Projects/Areas/Resources as appropriate).
3. Write a concise summary into today's daily note's `## Summary` section.
   ~3-6 sentences. Cover: what Jason captured, what got triaged, any
   patterns or stuck threads worth noting.
4. Commit with message `daily: review YYYY-MM-DD` and push.
5. DM Jason via `athena-notify` ONLY if there's something actionable
   (e.g., "I noticed two unresolved questions from earlier — want to address
   tomorrow?"). Otherwise stay quiet.
