Create today's daily note at `Daily/YYYY-MM-DD.md` (use today's date).

Minimal template:

```markdown
# YYYY-MM-DD

## Notes

## Summary
```

If the file already exists, do nothing. If not, create it, commit with
message `daily: seed YYYY-MM-DD`, and push.

Do not DM the user; this is a silent setup task.
