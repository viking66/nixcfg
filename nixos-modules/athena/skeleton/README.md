# Athena vault

Jason's second-brain / PKM workspace. Driven by Claude Code running in a
sandboxed VM on gordula; accessed from Telegram (primary) and the GitHub web
UI (browse and read).

## Structure

PARA + Inbox + Daily + an `_AI/` scratch area Claude owns.

- `Inbox/` — untriaged captures
- `Daily/YYYY-MM-DD.md` — daily notes
- `Projects/` — active projects
- `Areas/` — ongoing areas of responsibility
- `Resources/` — reference material
- `Archive/` — done/dormant
- `_AI/` — Claude's scratch, observations, schedule, session logs

See `_CLAUDE.md` for the working contract.

## Active projects

_None yet — starting fresh._

<!-- Claude maintains this list. Append a line per active project, remove when archived. -->
